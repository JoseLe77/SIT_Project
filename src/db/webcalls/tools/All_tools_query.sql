
SELECT tools.tool_num, tools.tool_name, tools.tool_url, CASE WHEN tools.tool_active = 1 THEN 'Enabled' ELSE 'Disabled' END tool_active FROM tools JOIN warehouses ON tools.wh_id = warehouses.wh_id WHERE tools.wh_id = '{}' and warehouses.wh_active = 1 ORDER BY tools.tool_num